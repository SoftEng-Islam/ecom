# Rules We must follow while writing the code

1. Follow Glassmorphism/Fluid in Front-end Designs
2. Add ideas and tasks into projectMap.md
3. Make done beside each task that already done
4. we use pug in template and We write all element attributes on one line
5. Follow the tailwindcss v4 not the old one make sure that u don't the old class names
6. I write the script at first of file than template than style like this:

   ```vue
   <script setup lang="ts"></script>
   <template lang="pug"></template>
   <style scoped lang="sass"></style>
   ```

7. comments for each function and method to explain and help other developers
8. Add meta tags and follow SEO/CLS Requirements
9. We use pnpm in this project and don't run or install just tell me and I will run and install that things you want
10. we write the html elements with classes like this in pug

```vue
<template lang="pug">
    div(class="bg-green-500")
</template>
```
